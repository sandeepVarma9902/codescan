{
  "name": "@codescan/cli",
  "version": "1.0.0",
  "description": "CodeScan CLI — review code files from the terminal or CI/CD",
  "bin": {
    "codescan": "./src/cli.js"
  },
  "type": "module",
  "scripts": {
    "dev": "node src/cli.js",
    "build": "ncc build src/cli.js -o dist"
  },
  "dependencies": {
    "@codescan/core": "workspace:*",
    "commander": "^11.0.0",
    "chalk": "^5.3.0",
    "ora": "^7.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.0"
  }
}
